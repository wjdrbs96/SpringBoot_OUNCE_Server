<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.gyun.ounce.mapper.SearchMapper">
    <select id="userSearch" resultType="UserProfileDto">
        SELECT u.id, p.profileIdx, p.profileURL, p.profileName, p.profileInfo
        FROM user u JOIN profile p ON u.userIdx = p.userIdx
        WHERE u.id LIKE concat('%',#{id},'%')
    </select>
</mapper>