<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.gyun.ounce.mapper.UserMapper">
    <select id="findById" resultType="User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <insert id="userInsert" useGeneratedKeys="true" keyColumn="userIdx">
        INSERT INTO user (id, password, email) VALUES (#{id}, #{password}, #{email})
    </insert>

    <select id="findByUserIdx" resultType="User">
        SELECT * FROM user WHERE userIdx = #{userIdx}
    </select>
</mapper>